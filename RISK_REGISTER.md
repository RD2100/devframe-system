# Risk Register

Date: 2026-06-15

| ID | Severity | Risk | Current control | Required next action |
|---|---|---|---|---|
| R-001 | P0 | `HUMAN_REQUIRED` or `BLOCKED` is reported as PASS | SADP negative fixtures and review rules; paper CLI final-acceptance labels, reviewer-pack non-authoritative boundary, production finalizer `final_acceptance` boundary pinned through `4ab02c8`, `agent-acceptance` SD-04 archive checks, SD-05 dispatch/test-frame/control-plane checks, and SD-06 business-validation checks pinned at `f3abb20` keep workflow/schema/artifact/report/test/zip/dispatch/business-validation status separate from final acceptance | Keep extending verdict-separation probes as new runtime artifacts appear. |
| R-002 | P0 | Dispatch success is treated as task success | Contract separation in plan; `agent-acceptance` SD-05 rejects dispatch/submission success promoted to final governance verdict; `devframe-control-plane` probe rejects `DispatchAssignment` records carrying final verdict fields | Require `ExecutionReport`, `ReviewVerdict`, and governance verdict as separate artifacts before live runtime use. |
| R-003 | P0 | Reviewer and executor roles are mixed | SADP reviewer rules | Enforce `independent_from_executor` in review contract. |
| R-004 | P0 | Worker writes outside approved scope | OpenCode post-run write-set hard gate pinned at `7a1278b`; goal runner reads run `state.json.changed_files`, checks effective `allowed_files`/`write_set`/`conflict_registry.write_set`, and fails forbidden or out-of-scope files with `diff_scope_ok=false` | Complete control-plane runtime SourceLock/WorkerLease enforcement before live runtime use. |
| R-005 | P1 | `agent-acceptance` active path drift | Documented in status | Decide whether to rebind active config to submodule path; do not rewrite historical archives. |
| R-006 | P1 | A120 ZIP evidence self-proves acceptance | Independent `PASS_WITH_BOUNDARY` ZIP review report now exists | Keep final acceptance separate from ZIP review; add negative fixtures before promotion. |
| R-007 | P1 | A120 evidence absent from submodule path | Canonical path documented | Record artifact path, hash, and owner in evidence manifest. |
| R-008 | P1 | `test-frame` is treated as plugin or final verdict source | Boundary docs | Keep role language consistent in docs and reports. |
| R-009 | P1 | Live CDP/Playwright capability is over-promoted | Human-gated status documented | Keep live runtime blocked until explicit RuntimeAuthorization exists. |
| R-010 | P1 | GitLab CI starts runtime/test jobs too early | Readonly CI policy | Keep initial CI limited to git, schema, and inventory checks. |
| R-011 | P1 | Paper private full text leaks into evidence, reports, or live WriteLab payloads | Paper runtime/API privacy gate pinned at `145fc05`; audit ZIP candidate sensitive scan pinned at `cb34be3`; live WriteLab sensitive-input guard pinned at `ea0758a`; redacted reviewer-pack boundary pinned at `51215f1`; finalizer acceptance boundary pinned through `4ab02c8`; archive SD-04/SD-05/SD-06 boundary pinned at `f3abb20`; paper audit privacy hard gate pinned at `8119c85`; Security Preflight P1 review pass pinned at `40ee21b`; machine-readable business validation report candidate pinned at `08ac4f5`; unauthorized raw `paragraph_text`/`writelab_token` fails closed to `human_required`, human-gate issue text is redacted, audit packaging rejects unredacted state/ledger/report candidates, direct live WriteLab calls block before HTTP dispatch without explicit `paragraph_text` authorization, report output redacts WriteLab payload fragments and Bearer tokens, business report schema keeps real-content authorization required, and graph/archive validation ignore reviewer-pack/artifact/dispatch/business-validation promotion claims | Keep real paper content blocked until fresh RuntimeAuthorization is issued and SD-07 is implemented. |
| R-012 | P1 | Paper schema/fixture user text becomes unreadable or structurally changed during cleanup | Narrow JSON/YAML parse plus diff review | Review only user-visible text changes; preserve fields, required list, enums, and schema shape. |
| R-013 | P1 | OpenCode post-run detection is mistaken for pre-write prevention | RuntimeAuthorization contract added in submodule branch | Require explicit `prevention_level`; do not claim filesystem sandbox until implemented. |
| R-014 | P2 | WriteLab handoff tests silently lose fixture coverage | Mitigated in `72d1dbd`: tracked `mock_handoff.zip`, refreshed manifest hashes, and added a ZIP/manifest consistency test; WriteLab adapter tests passed 63/63 and the broader paper/WriteLab group passed 221/221 | Keep the handoff ZIP validation tests in CI and add future negative fixtures for privacy-attestation and integrity failures. |
| R-015 | P1 | TaskSpec verification commands become unsafe execution input | Reviewed pass with boundary at `dev-frame-opencode` commit `40ee21b`: TaskSpec `verify` uses a deny-by-default allowlist and rejects shell operators, inline Python, unknown executables, and write/fix flags. Main-thread P1 regression suite and independent review passed. | Keep verification command execution deny-by-default; consider structured verification schema in a future hardening slice. |
| R-016 | P1 | OpenCode exit status is treated as safety verdict | Reviewed pass with boundary at `dev-frame-opencode` commit `40ee21b`: `goal_review_recovered --apply` requires structured JSON verdict and independent `diff_scope_ok=true` plus `reviewed_diff=true` gate evidence. Main-thread P1 regression suite and independent review passed. | Keep exit code, recovery status, and final acceptance separated. |
| R-017 | P1 | Daemon queued write execution lacks daemon-side risk gate | Reviewed pass with boundary at `dev-frame-opencode` commit `40ee21b`: naked `daemon_write_authorized` self-authorization no longer passes; queued coding writes require structured `daemon_authorization` bound to task/project/workflow/source and unexpired human gate evidence. Main-thread daemon regression suite and independent review passed. | Before live daemon worker use, strengthen provenance with signed or out-of-band authorization storage. |
| R-018 | P2 | Unsigned audit bundle or polluted queue status is promoted by downstream consumers | Security Preflight findings `SEC-AUDIT-001` and `SEC-QUEUE-001` recorded | Make unsigned status non-promotable and enforce queue transition matrix. |
| R-019 | P1 | Synthetic/offline paper business validation is mistaken for final product acceptance | `dev-frame-opencode` commit `08ac4f5` adds `paper business-validate`, JSON schema, tests, and docs for candidate-only machine-readable reporting; `test-frame` commit `891b106` adds `NEG-039` through `NEG-048`; `agent-acceptance` commit `f3abb20` rejects business-validation promotion to final governance verdict | Require human review plus fresh RuntimeAuthorization before real paper content, live WriteLab flow, or final acceptance. |
| R-020 | P1 | Real-content or live WriteLab pilot bypasses fresh RuntimeAuthorization | `agent-acceptance` read-only SD-07 review returned `SD07_TASKSPEC_REQUIRED`; current report artifact keeps `runtime_authorization_required_for_real_content=true` and `validation_mode=synthetic_offline` | Implement SD-07 closure validator/negative fixtures before any real-content pilot or live WriteLab execution. |
